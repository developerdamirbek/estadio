export const ROUTES = {
  home: '/',
  courses: '/courses',
  blogs: '/blogs',
  singleBlog: '/blogs/:id',
  singleCourse: '/courses/:id',
  certificate: '/certificate',
}

export const ROUTES_LINKS = Object.values(ROUTES)
